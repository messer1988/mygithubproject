apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "nginx-app.fullname" . }}-site
data:
  index.html: |
    <!DOCTYPE html>
    <html lang="en">
    <head>
      <meta charset="UTF-8" />
      <title>DevOps Helper</title>
      <style>
        body {
          font-family: system-ui, sans-serif;
          background: #0f172a;
          color: #e5e7eb;
          margin: 0;
          padding: 0;
        }
        header {
          background: #020617;
          padding: 20px;
          text-align: center;
        }
        main {
          padding: 40px;
          max-width: 900px;
          margin: auto;
        }
        .cards {
          display: grid;
          grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
          gap: 20px;
        }
        .card {
          background: #020617;
          padding: 20px;
          border-radius: 12px;
        }
        footer {
          text-align: center;
          padding: 20px;
          opacity: 0.6;
        }
      </style>
    </head>
    <body>
      <header>
        <h1>🚀 DevOps Helper</h1>
        <p>Kubernetes • Helm • Istio • CI/CD</p>
      </header>

      <main>
        <div class="cards">
          <div class="card">
            <h3>⚙️ Kubernetes</h3>
            <p>Pods, Deployments, Services, Probes</p>
          </div>
          <div class="card">
            <h3>🧩 Istio</h3>
            <p>Gateway, VirtualService, mTLS</p>
          </div>
          <div class="card">
            <h3>📦 Config</h3>
            <p>ConfigMap + envsubst</p>
          </div>
        </div>
      </main>

      <footer>
        Made by Python • DevOps Playground
      </footer>
    </body>
    </html>