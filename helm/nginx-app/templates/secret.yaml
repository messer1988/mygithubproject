apiVersion: v1
kind: Secret
metadata:
  name: {{ include "helm.nginx-app.fullname" . }}-secret
  labels:
    {{- include "helm.nginx-app.labels" . | nindent 4 }}
type: Opaque
stringData:
{{- range $k, $v := .Values.env.secret }}
  {{ $k }}: {{ $v | quote }}
{{- end }}

