1. Control Plane OpenShift

OpenShift наследует Kubernetes control plane, но добавляет свои расширения:
	•	kube-apiserver — сердце (как в Kubernetes).
	•	etcd — хранение состояния.
	•	kube-controller-manager, kube-scheduler — планирование, контроль состояния.
	•	OpenShift API Server — расширение API Kubernetes для работы с Route, Project, BuildConfig, ImageStream и др.
	•	OpenShift Controller Manager — управляет специфичными объектами (Build, DeploymentConfig, Route).

⸻

2. Node (Worker Node)

Состоит из:
	•	kubelet — запускает Pod.
	•	kube-proxy — маршрутизация Service.
	•	CRI-O — стандартный контейнерный рантайм (вместо containerd/Docker).
	•	SDN (Software Defined Network) — OpenShift использует OVN-Kubernetes (раньше OpenShift SDN) для сетки Pod’ов.

⸻

3. Ключевые сущности OpenShift (отличие от Kubernetes)

🔹 Projects (Namespace)
	•	OpenShift использует Project вместо Namespace.
	•	Добавлены квоты, роли и ограничения для пользователей.

🔹 DeploymentConfig
	•	Расширение Deployment из Kubernetes.
	•	Позволяет тонко управлять rollout’ами и триггерами (например, автоматически обновлять Pod при изменении ImageStream).

🔹 Route
	•	Альтернатива Ingress.
	•	Позволяет экспонировать Service наружу через HTTP(S).
	•	Может делать TLS termination: Edge, Passthrough, Re-encrypt.

🔹 BuildConfig + ImageStream
	•	BuildConfig — описывает процесс сборки образа (Source-to-Image, Docker build).
	•	ImageStream — абстракция над образом. Позволяет триггерить Deployment при обновлении образа.

🔹 SCC (Security Context Constraints)
	•	Расширение PodSecurity из Kubernetes.
	•	Контролирует, с какими правами может запускаться контейнер (UID, root, volume types).

⸻

4. Пользовательский запрос: путь от клиента до Pod

Давай пройдём путь пользователь → приложение в OpenShift:
	1.	Пользователь обращается к URL https://hello-demo.apps-crc.testing.
	2.	DNS резолвит домен → IP ingress router (HAProxy, встроенный в OpenShift).
	3.	Router (Ingress Controller в OpenShift) смотрит правила Route:
	•	Host = hello-demo.apps-crc.testing
	•	Backend = Service hello
	4.	Service перенаправляет запрос к Endpoints (Pod’ам).
	5.	Pod (контейнер nginx) получает запрос.
	•	ConfigMap и Secret подставляют конфиги и пароли.
	•	PVC обеспечивает хранение (если нужно).
	•	SCC проверяет безопасность Pod.
	6.	Pod возвращает ответ через Service → Router → клиенту.

⸻

5. DeploymentConfig — подробная работа

Аналогично тому, как мы разбирали Deployment в Kubernetes:
	•	Состоит из:
	•	replicas
	•	selector
	•	template (Pod)
	•	strategy (Recreate/Rolling)
	•	triggers (по коду/образу/конфигу)
	•	Механика:
	•	DeploymentConfig управляет ReplicaSet-подобными сущностями (ReplicationController).
	•	Поддерживает автоматические триггеры: например, при обновлении ImageStream → новый rollout.
	•	Взаимодействие:
	•	DeploymentConfig → ReplicationController → Pod.
	•	Триггеры могут быть связаны с BuildConfig и ImageStream.

⸻

6. BuildConfig + ImageStream — CI/CD внутри OpenShift
	•	BuildConfig может брать код из Git, собирать образ (через Source-to-Image или Dockerfile) и публиковать в ImageStream.
	•	ImageStream хранит версию образа.
	•	Если ImageStream обновился → DeploymentConfig автоматически перезапускает Pod.

👉 Таким образом, в OpenShift встроен мини-CI/CD без Jenkins.

⸻

7. Безопасность
	•	SCC (Security Context Constraints) → определяет права Pod (какие volume можно монтировать, запуск от root и т.д.).
	•	RBAC → управление правами пользователей.
	•	OAuth → интеграция с LDAP, GitHub, Google для входа.

⸻

🧩 Итог: Взаимодействие всех компонентов OpenShift
	•	Пользователь → DNS → Router (HAProxy) → Route → Service → Pod.
	•	Pod использует ConfigMap/Secret/PVC.
	•	Pod управляется DeploymentConfig (или Deployment).
	•	Образ приходит через BuildConfig → ImageStream.
	•	Безопасность проверяется через SCC и RBAC.
	•	Управление всем этим идёт через OpenShift API Server.