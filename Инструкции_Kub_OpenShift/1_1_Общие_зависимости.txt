#общее обновление
brew update

# Docker Desktop (нужен для Minikube/Kind и образов )
brew install --cask docker
open -a Docker

# kubectl + helm + OpenShift-cli
brew install kubectl helm openshift-cli
kubectl version --client ( узнать версию kubectl)
helm version ( узнать версию helm )
oc version --client ( узнать версию openShift)