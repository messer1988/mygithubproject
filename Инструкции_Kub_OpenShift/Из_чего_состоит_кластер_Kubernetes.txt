Из чего состоит кластер Kubernetes

Контрольная плоскость (Control Plane)
	•	kube-apiserver — единственная «дверь» в кластер. Все клиенты (kubectl, контроллеры, операторы) говорят только с API-сервером.
	•	etcd — распределённое KV-хранилище истинного состояния (декларативные манифесты, статусы).
	•	kube-scheduler — выбирает, на какой Node поместить новый Pod (учитывая ресурсы, affinity/taint и т.п.).
	•	kube-controller-manager — набор контроллеров (Deployment/ReplicaSet, Node, EndpointSlice, Job и др.), которые приводят текущее состояние к желаемому.
	•	cloud-controller-manager (если есть облачный провайдер) — интеграции с LB, дисками, адресами и т.д.

Узлы (Worker Nodes)
	•	kubelet — агент на ноде; гарантирует, что Pod’ы, которых хочет API, реально запущены через контейнерный рантайм (CRI).
	•	kube-proxy — программирует сетевые правила (iptables/ipvs) для Service, балансируя трафик к Endpoints.
	•	Container Runtime (containerd/CRI-O) — запускает контейнеры, распределяет образы.
	•	CNI-плагин (Calico/Flannel/Cilium и т.д.) — сшивает сетку Pod’ов (Pod CIDR), даёт им адреса и маршрутизацию.
	•	CoreDNS — DNS внутри кластера (svc-имена → ClusterIP).

Базовые сущности (рабочие нагрузки)
	•	Pod — минимальная единица запуска (один/несколько контейнеров + сеть/файловая система).
	•	ReplicaSet — обеспечивают N одинаковых Pod’ов.
	•	Deployment — управляет ReplicaSet’ами (обновления/масштабирование; rolling update).
	•	StatefulSet — как Deployment, но с устойчивой идентичностью Pod’ов и упорядоченным стартом/стопом (для БД/кластеров).
	•	DaemonSet — по одному Pod на каждую ноду (агенты/логгеры).
	•	Job/CronJob — разовые/периодические задачи.
Сетевой доступ и маршрутизация
	•	Service
	•	ClusterIP — внутренняя точка входа (по умолчанию).
	•	NodePort — открывает порт на каждой ноде.
	•	LoadBalancer — заказывает внешний LB (в облаке).
	•	EndpointSlice — список адресов/портов живых Pod’ов за Service (влияет на балансировку).
	•	Ingress + Ingress Controller — L7-маршрутизация HTTP(S) извне в Service (host/path правила). В OpenShift аналог — Route.

Конфигурация и безопасность
	•	ConfigMap / Secret — конфигурация и секреты как ENV/файлы (тома).
	•	ServiceAccount + RBAC — права для Pod’ов и пользователей.
	•	NetworkPolicy — L3/L4-политики между Pod’ами.
	•	SecurityContext / PodSecurity — права пользователей, файловой системы, линукс-капы и т.п.
	•	PVC/PV/StorageClass — постоянное хранилище.

Наблюдаемость и автоматика
	•	Probes — readiness, liveness, startup.
	•	HPA/VPA — авто-масштабирование по метрикам.
	•	Metrics Server / Prometheus — метрики кластера/приложений.


1. Pod

📌 Что это
Минимальная единица запуска в Kubernetes. Под = один или несколько контейнеров, которые делят:
	•	сеть (один IP на Pod)
	•	volume (файловую систему, если смонтированы)

📂 Из чего состоит
	•	containers: список контейнеров (образ, порты, команды)
	•	volumes: тома (emptyDir, ConfigMap, Secret, PVC и т.д.)
	•	metadata.labels: метки, по которым Pod привязывается к Service/ReplicaSet
	•	status: IP, фаза (Pending/Running/CrashLoopBackOff), условия готовности

⚙️ Как работает
Pod живёт только вместе с нодой. Если нода падает → Pod умирает (ReplicaSet/Deployment создаст новый на другой ноде).

🔗 Взаимодействия
	•	Pod ↔ Service: Service использует метки Pod для балансировки
	•	Pod ↔ ConfigMap/Secret: монтируются в ENV или файлы
	•	Pod ↔ kubelet: kubelet следит, что контейнеры работают

2. ReplicaSet

📌 Что это
Контроллер, который гарантирует, что всегда запущено заданное количество Pod’ов.

📂 Из чего состоит
	•	replicas: сколько Pod держать
	•	selector: по каким меткам считать Pod «своими»
	•	template: шаблон Pod (но обычно им управляет Deployment)

⚙️ Как работает
	•	Если Pod удалился/упал → ReplicaSet создаст новый
	•	Если Pod добавлен вручную без метки → ReplicaSet его не видит

🔗 Взаимодействия
	•	ReplicaSet ← управляется Deployment
	•	ReplicaSet → управляет Pod’ами

⸻

3. Service

📌 Что это
Стабильная точка входа для Pod’ов. У Pod’ов IP меняются, у Service — постоянный ClusterIP.

📂 Типы Service
	•	ClusterIP (по умолчанию) — доступен только в кластере
	•	NodePort — открывает порт на каждой ноде
	•	LoadBalancer — подключает внешний LB (в облаке)
	•	ExternalName — DNS-алиас

⚙️ Как работает
	•	kube-proxy на каждой ноде прописывает правила iptables/ipvs
	•	При запросе на ClusterIP трафик перенаправляется к случайному Pod из Endpoints

🔗 Взаимодействия
	•	Service ↔ Endpoints: список IP готовых Pod’ов
	•	Service ↔ Ingress: Ingress всегда направляет в Service, а не в Pod напрямую

⸻

4. Ingress

📌 Что это
HTTP/HTTPS-маршрутизатор. Правила (host/path) → какой Service использовать.

📂 Из чего состоит
	•	spec.rules: хосты/пути и куда их направлять
	•	spec.tls: SSL-сертификаты (Secret)

⚙️ Как работает
	•	Пользователь идёт на https://site.com
	•	DNS резолвит в IP Ingress Controller (nginx, traefik и т.д.)
	•	Контроллер смотрит правила Ingress и перенаправляет в Service

🔗 Взаимодействия
	•	Ingress → Service
	•	Ingress → TLS Secret (для HTTPS)

(В OpenShift вместо Ingress = Route)

⸻

5. ConfigMap

📌 Что это
Хранилище не секретных конфигураций (строки, key-value).

📂 Из чего состоит
	•	data: пары ключ=значение
	•	binaryData: бинарные данные в base64

⚙️ Как работает
Может монтироваться в Pod как:
	•	ENV (envFrom.configMapRef)
	•	Файлы (volumeMounts)

🔗 Взаимодействия
	•	ConfigMap → Pod (ENV или файл)
	•	Если ConfigMap изменился, ENV не обновятся автоматически (нужен рестарт Pod).

⸻

6. Secret

📌 Что это
Хранилище конфиденциальных данных (пароли, токены, ключи).

📂 Из чего состоит
	•	type: Opaque, dockerconfigjson, tls и др.
	•	data: base64-значения

⚙️ Как работает
	•	Подключается в Pod как ENV или файл
	•	Тип tls используется в Ingress для HTTPS

🔗 Взаимодействия
	•	Secret → Pod (пароли, ключи)
	•	Secret → Ingress (TLS)

⸻

7. PersistentVolume / PersistentVolumeClaim

📌 Что это
Хранилище данных, независимое от Pod’ов.

📂 Из чего состоит
	•	PV: физический ресурс (диск, NFS, cloud volume)
	•	PVC: запрос от Pod на определённый размер/тип

⚙️ Как работает
Pod через PVC получает доступ к PV. Если Pod пересоздаётся, данные сохраняются.

🔗 Взаимодействия
	•	PVC → Pod (монтируется как volume)
	•	PVC → PV (связывается автоматически по StorageClass)

⸻

8. Node

📌 Что это
Физическая или виртуальная машина в кластере.

📂 Компоненты
	•	kubelet — управляет Pod’ами
	•	kube-proxy — настраивает сетевые правила
	•	runtime (containerd/CRI-O) — запускает контейнеры
	•	CNI-плагин — даёт Pod’ам сеть

⚙️ Как работает
Node получает задачи от API (через kubelet), запускает Pod, сообщает статус.

🔗 Взаимодействия
	•	Node ↔ Pod (kubelet следит)
	•	Node ↔ Control Plane (через API-сервер)

⸻

9. kube-proxy

📌 Что это
Компонент на каждой ноде, который управляет правилами сетевой маршрутизации для Service.

⚙️ Как работает
	•	Следит за изменениями Service/Endpoints
	•	Программирует iptables/ipvs
	•	Делает round-robin балансировку между Pod’ами

⸻

10. CoreDNS

📌 Что это
DNS-сервер внутри кластера.

⚙️ Как работает
	•	Все сервисы получают DNS-имя: <service>.<namespace>.svc.cluster.local
	•	Pod может обратиться по имени сервиса вместо IP