База знаний по терминам Kubernetes и OpenShift
🔹 Основы Kubernetes
Cluster (Кластер) — группа узлов (Node), управляемая Kubernetes.
Node (Узел) — физический или виртуальный сервер в кластере, где запускаются контейнеры.
Pod — минимальная единица, в которой работает контейнер (или несколько). Под = контейнер + сеть + тома.
Namespace — логическая изоляция ресурсов внутри кластера (например, dev, prod).
Deployment — контроллер, управляющий Pod’ами (создание, масштабирование, обновления).
ReplicaSet — объект, который гарантирует нужное количество Pod’ов.
Service — абстракция для доступа к Pod’ам. Балансирует трафик. Типы:
ClusterIP – доступен только внутри кластера.
NodePort – доступен снаружи через порт на Node.
LoadBalancer – интеграция с облачным балансировщиком.
Ingress — прокси, который маршрутизирует HTTP/HTTPS запросы к сервисам (часто через Nginx/HAProxy/Traefik).
ConfigMap — хранение конфигураций в виде ключ/значение.
Secret — хранение чувствительных данных (пароли, ключи).
Volume — диск, подключаемый к Pod’у (например, для хранения данных).
StatefulSet — как Deployment, но для stateful-приложений (БД, Kafka и т. д.), с сохранением идентичности Pod’ов.
DaemonSet — гарантирует, что Pod запущен на каждом Node (например, для агента мониторинга).
Job / CronJob — одноразовые или периодические задачи.

🔹 Дополнительно в Kubernetes
Kubelet — агент, который работает на каждом Node и запускает контейнеры.
kube-proxy — прокси для маршрутизации сетевого трафика внутри кластера.
etcd — key-value база данных, в которой Kubernetes хранит все состояния кластера.
API Server — центральная точка управления (все команды kubectl идут сюда).
Controller Manager — следит за объектами и приводит систему к нужному состоянию.
Scheduler — решает, на какой Node запустить Pod.

🔹 OpenShift (надстройка над Kubernetes)
OpenShift = Kubernetes + удобства для разработчиков и DevOps.
Основные особенности:
Project — то же самое, что Namespace в Kubernetes, но с дополнительными политиками.
Route — аналог Ingress в Kubernetes. Делает сервис доступным извне через HTTP/HTTPS.
Source-to-Image (S2I) — автоматическая сборка образа из исходного кода (без Dockerfile).
BuildConfig — описание процесса сборки (например, из Git-репо).
ImageStream — трекер версий Docker-образов внутри OpenShift.
DeploymentConfig — аналог Deployment, но с дополнительными возможностями (хуки, стратегии развёртывания).
Operator — способ упаковать знание о приложении (например, Postgres Operator управляет кластерами PostgreSQL).
ServiceAccount — учётная запись для Pod’ов с доступом к API.
Role / RoleBinding — права доступа к ресурсам (RBAC).
ClusterRole / ClusterRoleBinding — права на весь кластер.